name: Lint

on:
  pull_request:
    branches:
      - master

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Prettier
        run: yarn prettier --list-different
      - name: Stylelint
        run: yarn workspace infima lint
